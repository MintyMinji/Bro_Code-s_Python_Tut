
public class Main{
    public static void main(String[] args) {

        double number;

        System.out.println(Math.PI);
        //number = Math.pow(2, 5); // raise base number to a power
        //number = Math.abs(-5); //absolute value (gives us a number but positive)
        //number = Math.sqrt(9); //square root)
        //number = Math.round(3.14); //rounding numbers
        //number = Math.ceil(3.14); // round upwards 
        //number = Math.floor(3.99); // round downwards
        //number = Math.max(10, 20); // compare max between 2 values
        number = Math.min(30, 40); // compare minimum between 2 values 


        System.out.println(number);

    }
}
